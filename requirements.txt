# Tier 1 (closed-form screening) only needs these:
numpy>=1.24
scipy>=1.10

# Tier 2 (coupled FE) additionally needs:
scikit-fem>=8.0
meshio>=5.3
pygmsh>=7.1
gmsh>=4.11
matplotlib>=3.7

# Tests:
pytest>=7.0

# NOTE: the gmsh wheel links against X11/GL shared libraries even when run
# headless. On a bare Debian/Ubuntu image also install:
#   apt-get install -y libglu1-mesa libxft2 libxinerama1 libxcursor1 \
#                      libxrender1 libxfixes3 libfontconfig1
